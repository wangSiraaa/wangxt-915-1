version: '3.8'

services:
  visitor-gate:
    build: .
    container_name: visitor-gate-system
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    volumes:
      - ./data:/app/data
    restart: unless-stopped
